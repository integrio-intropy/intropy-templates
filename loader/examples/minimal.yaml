# Render fixture: full sample (business logic + service wiring).
#   intropy int create loader -o /tmp/loader-out -f loader/examples/minimal.yaml --version main --no-input
name: OrderLoader
organization: Fluxia
subscribes: orders
# The channel and payload type are projections of the subscribes value:
# topic 'orders', payload type 'Orders'.
empty: false
