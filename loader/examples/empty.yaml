# Render fixture: empty bodies (steps throw NotImplementedException; the
# idempotency/business-incident wiring stays — loader idempotency keys off
# CloudEvent Subject/Time, so there are no lambdas to stub) — the
# migration-agent starting point.
#   intropy int create loader -o /tmp/loader-empty -f loader/examples/empty.yaml --version main --no-input
name: OrderLoader
organization: Fluxia
message: orders
# The channel and payload type derive from the message: topic 'orders',
# type 'Orders'. `contract` overrides the derived type name when it reads badly.
empty: true
