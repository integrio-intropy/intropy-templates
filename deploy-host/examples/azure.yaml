# Azure: Service Bus topics. Credentials come from a Kubernetes Secret the
# platform syncs out of Key Vault; the template only declares the Secret name
# and key the pubsub Component resolves.
domain: sales
system: ordersync
pubsub: servicebus
pubsubName: pubsub
