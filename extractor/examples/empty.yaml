# Render fixture: empty bodies (steps throw NotImplementedException; the
# idempotency/business-incident wiring stays with throwing extractor lambdas)
# — the migration-agent starting point.
#   intropy int create extractor -o /tmp/extractor-empty -f extractor/examples/empty.yaml --version main --no-input
name: OrderExtractor
organization: Fluxia
message: orders
# topic and payload type derive from the message: channel 'orders', type 'Orders'.
# The `topic`/`contract` parameters are overrides for leaving those defaults.
empty: true
