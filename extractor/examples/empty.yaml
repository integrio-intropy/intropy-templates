# Render fixture: empty bodies (steps and rules throw NotImplementedException)
# — the migration-agent starting point.
#   intropy int create extractor -o /tmp/extractor-empty -f extractor/examples/empty.yaml --version main --no-input
name: OrderExtractor
organization: Fluxia
publishes: orders
# The channel and payload type are projections of the publishes value:
# topic 'orders', payload type 'Orders'.
empty: true
