# Render fixture: full sample (business logic in the steps and rules).
#   intropy int create extractor -o /tmp/extractor-out -f extractor/examples/minimal.yaml --version main --no-input
name: OrderExtractor
organization: Fluxia
publishes: orders
# The channel and payload type are projections of the publishes value:
# topic 'orders', payload type 'Orders'.
empty: false
