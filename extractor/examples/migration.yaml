# Render fixture: migration — preserves an existing CloudEvent source
# instead of accepting the derived default. The message identity has no
# override: `publishes` names the message, and its name IS the CloudEvents
# type. Only the envelope source is pinnable.
name: Extractor.Perfion
organization: Maxbo
publishes: product-export
eventSource: "urn:maxbo:perfion"
empty: false
