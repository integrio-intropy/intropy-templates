{{- if .empty -}}
namespace {{ .name }}.Model;

public record Source{{ .payloadType }}();
{{- else -}}
using {{ .contracts }};

namespace {{ .name }}.Model;

public record Source{{ .payloadType }}(
    string OrderId,
    string CustomerId,
    DateTimeOffset OrderDate,
    IReadOnlyList<OrderLine> Lines);
{{- end }}
