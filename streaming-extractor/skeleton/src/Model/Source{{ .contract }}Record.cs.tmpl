namespace {{ .name }}.Model;


/// <summary>
/// The source shape of one streamed record — the inbound file's per-record
/// shape, local to this component (the published reference contract lives in
/// the sibling <c>{{ .contracts }}</c> project). Product-flavored sample.
/// </summary>
public record Source{{ .contract }}Record(
    string Sku,
    string Name,
    decimal Price);
