namespace {{ .name }}.Model;


/// <summary>
/// The output shape of one streamed record — what the process step writes as
/// one JSONL line to the file's output. Product-flavored sample.
/// </summary>
public record {{ .contract }}Record(
    string Sku,
    string Name,
    decimal Price);
