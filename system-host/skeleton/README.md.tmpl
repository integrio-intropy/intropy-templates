# {{ .name }} SystemHost

The Aspire host of the `{{ .name }}` Intropy integration system. One class —
`{{ .systemClass }}` — declares which components exist and which topics connect
them; everything the host does is a backend over that declaration.

## Run

```bash
task run        # the whole system under the Aspire dashboard; Ctrl+C to stop
```

Extractors are run-to-completion jobs: a host run executes each one once at
startup and, when the block declares `.WithSchedule("<cron>")` in `Define()`,
re-runs it on every cron tick — so you can drop files into inbound folders
while the system is live. Overlapping ticks are skipped, and each scheduled
component has a "Run now" command in the dashboard. Without a schedule, seed
inbound folders before starting. Loaders are long-running services and stay up
for the whole host run.

## Trigger a component

```bash
task seed       # drop the sample order into the connector folder (extractors sweep it)
task publish    # publish the sample order to the topic through a running sidecar
```

`task seed CONNECTOR_DIR=./test/<connector>` targets the folder the connector
resolves to in the development definition. `task publish` takes overrides for
the publishing component (`PUBLISH_APP_ID`) and the topic (`TOPIC`) — the dapr
CLI resolves the sidecar from the app-id.

## Inspect the model without starting anything

```bash
task check      # validate the topology
task graph      # print the SystemTopology JSON
task generate   # write Dapr YAML + per-component runtime config to ./out
```

## Layout

- `{{ .systemClass }}.cs` — the system definition (`ISystemDefinition`)
- `Topics.cs` — the system's topics as typed refs
- `Connectors.cs` — the system's connectors (local file folders under `test/`
  by default; point them at real external systems when known)
- `sample-data/sample-order.json` — the sample order used by `task seed` and
  `task publish`
- `../{{ .projectName }}.Contracts/` — the contract records the topics carry
  (generated by `intropy sys create`)
- `Program.cs` — one entry point, two backends; not meant to be edited

Components live as sibling folders of this project, one folder per component,
folder name == component name == Dapr app-id. A freshly scaffolded host has no
components yet: `task check` reports ITP002 until `Define()` declares them.
