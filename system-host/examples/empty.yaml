# Render fixture: an empty system — the payload lists at their minimum. The
# render compiles; `dotnet run -- check` reports ITP002 until components are
# declared.
#   intropy int create system-host -o /tmp/system-host-empty -f system-host/examples/empty.yaml --version main --no-input
name: order-flow
topics: []
connectors: []
components: []
sharedContracts:
  name: Contracts
  include: ../Contracts/Contracts.csproj
