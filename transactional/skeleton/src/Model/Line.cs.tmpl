namespace {{ .name }}.Model;

{{- if .empty }}

// The line item record of the sample contract. Add it when In takes its real
// shape; its Lines property references this record.
{{- else }}

public record Line(
    string Sku,
    int Quantity,
    decimal UnitPrice);
{{- end }}
